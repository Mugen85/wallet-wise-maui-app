<!-- Views/DashboardPage.xaml -->
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:WalletWise.ViewModels"
             xmlns:services="clr-namespace:WalletWise.Services" 
             x:Class="WalletWise.Views.DashboardPage"
             x:DataType="viewmodels:DashboardViewModel"
             Title="Dashboard">

    <ScrollView>
        <VerticalStackLayout Spacing="20" Padding="20">

            <Frame Style="{StaticResource InfoCard}">
                <VerticalStackLayout Spacing="10">
                    <Label Text="Patrimonio Totale" Style="{StaticResource CardTitle}" />
                    <Label Text="{Binding TotalBalance, StringFormat='{0:C}'}" Style="{StaticResource CardAmount}" />
                </VerticalStackLayout>
            </Frame>

            <!-- Sezione Budget -->
            <Frame Style="{StaticResource InfoCard}">
                <VerticalStackLayout Spacing="15">
                    <Label Text="Situazione Budget Mensile" Style="{StaticResource CardTitle}" />

                    <VerticalStackLayout BindableLayout.ItemsSource="{Binding BudgetSummary}" Spacing="15">
                        <BindableLayout.ItemTemplate>
                            <DataTemplate x:DataType="services:BudgetStatus">
                                <!-- TEST 2: Aggiungiamo la ProgressBar. -->
                                <VerticalStackLayout Padding="0,10" Spacing="5">
                                    <Label Text="{Binding Category}" FontAttributes="Bold" />
                                    <ProgressBar Progress="{Binding Progress}" ProgressColor="{StaticResource Primary}" />
                                </VerticalStackLayout>
                            </DataTemplate>
                        </BindableLayout.ItemTemplate>
                    </VerticalStackLayout>

                </VerticalStackLayout>
            </Frame>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
<!-- in WalletWise/Views/AddBudgetPage.xaml -->
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="WalletWise.Views.AddBudgetPage"
             xmlns:viewmodels="clr-namespace:WalletWise.ViewModels"
             x:DataType="viewmodels:AddBudgetViewModel"
             Title="Aggiungi Budget">
    <Grid Padding="20" RowSpacing="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Text="Categoria di spesa" />
        <Picker Grid.Row="1" 
                ItemsSource="{Binding Categories}"
                SelectedItem="{Binding SelectedCategory}"
                ItemDisplayBinding="{Binding .}"/>     


        <Label Grid.Row="2" Text="Importo budget mensile" />
        <Entry Grid.Row="3" Text="{Binding Amount, Converter={StaticResource DecimalConverter}}" Keyboard="Numeric" />

        <!-- INIZIO MODIFICA: Raggruppiamo i bottoni in fondo -->
        <VerticalStackLayout Grid.Row="4" VerticalOptions="End" Spacing="10">
            <Button Text="Salva Budget" Command="{Binding SaveBudgetCommand}" Style="{StaticResource PrimaryButton}" />
            <Button Text="Annulla" Command="{Binding CancelCommand}" Style="{StaticResource SecondaryButton}" />
        </VerticalStackLayout>
        <!-- FINE MODIFICA -->
    </Grid>
</ContentPage>